apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qapps.fullname" . }}-test-connection"
  labels:
    {{- include "qapps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qapps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
