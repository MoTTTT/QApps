apiVersion: v1
kind: Service
metadata:
  name: {{ include "qapps-zope.fullname" . }}
  labels:
    {{- include "qapps-zope.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qapps-zope.selectorLabels" . | nindent 4 }}
