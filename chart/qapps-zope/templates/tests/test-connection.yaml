apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qapps-zope.fullname" . }}-test-connection"
  labels:
    {{- include "qapps-zope.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qapps-zope.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
